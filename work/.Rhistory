}
f(data2$Q1)
f <- function(x, ...) {
a <- table(x, ...)
b <- prop.table(a, ...)
a
b
}
f(data2$Q1)
f <- function(x, ...) {
a <- table(x, ...)
b <- prop.table(a, ...)
print(a)
print(b)
}
f(data2$Q1)
t <- function(x, ...) {
a <- table(x, ...)
b <- prop.table(a, ...)
print(a)
print(b)
}
t(data2$Q1)
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...)
b <- prop.table(a, ...)
print(a)
print(b)
}
with(data2, t(Q1, Q2))
t(data2$Q1)
t <- function(x, ...) {
a <- table(x, ...)
b <- prop.table(table(x, ...))
print(a)
print(b)
}
t(data2$Q1)
with(data2, t(Q1, Q2))
with(data2, t(x = Q1, Q2))
t(data2$Q1)
with(data2, prop.table(table(Q1, Q2), margin = 1))
with(data2, prop.table(table(Q1, Q2), margin = 2))
t2 <- function(x, ...) {
with(data2, table(x))
}
t2(data2$Q1)
t2(Q1)
t2([Q1])
t2[Q1]
t2 <- function(x, ...) {
with(data2, table(x))
}
t2[Q1]
t2(Q1)
with(data2, t(Q1, Q2), margin = 1)
with(data2, t(Q1, Q2), margin = 2)
with(data2, t(Q1, Q2), margin = 1)
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
b <- prop.table(table(x, ...), ...) # proportions
print(a)
print(b)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
b <- prop.table(table(x, ...)) # proportions
print(a)
print(b)
}
with(data2, t(Q1, Q2))
with(data2, t(Q1, Q2), margin = 1)
with(data2, t(Q1, Q2, margin = 1)
)
t <- function(x, ...) {
a <- table(x, ...) # counts
b <- prop.table(table(x, ...)...) # proportions
print(a)
print(b)
t <- function(x, ...) {
a <- table(x, ...) # counts
b <- prop.table(table(x, ...)...) # proportions
t <- function(x, ...) {
a <- table(x, ...) # counts
b <- prop.table(a, ...) # proportions
print(a)
print(b)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
b <- prop.table(a, ...) # proportions
print(a)
print(b)
}
View(t)
with(data2, t(Q1, Q2))
t2 <- function(x, ...) {
table(x, ...)
prop.table(table(x, ...))
}
with(data2, t2(Q1, Q2))
with(data2, t2(Q1, Q2), margin = 1)
with(data2, t2(Q1, Q2), margin = 2)
t2 <- function(x, ...) {
prop.table(table(x, ...))
}
with(data2, t2(Q1, Q2), margin = 2)
t2 <- function(x, ...) {
prop.table(table(x), ...)
}
with(data2, t2(Q1, Q2), margin = 2)
with(data2, t2(Q1, Q2))
t2 <- function(x, ...) {
prop.table(table(x, ...))
}
with(data2, t2(Q1, Q2), margin = 1)
with(data2, t2(Q1, Q2), margin = 2)
with(data2, prop.table(table(Q1, Q2), margin = 1))
with(data2, prop.table(table(Q1, Q2), margin = 2))
with(data2, prop.table(table(Q1)))
with(data2, prop.table(table(Q1, Q2)))
t2 <- function(x, ...) {
a = prop.table(table(x, ...))
}
with(data2, t2(Q1, Q2), margin = 2)
a
t2 <- function(x, ...) {
a = prop.table(table(x, ...))
a
}
with(data2, t2(Q1, Q2), margin = 2)
with(data2, t2(Q1, Q2), margin = 1)
with(data2, t(Q1, Q2), margin = 1)
t <- function(x, ...) {
a <- table(x, ...) # counts
t <- function(x, ...) {
a <- table(x, ...) # counts
t <- function(x, ...) {
a <- table(x, ...) # counts
b <- prop.table(a, ...) # proportions
print(a)
print(b)
}
with(data2, t(Q1, Q2), margin = 1)
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
b <- prop.table(t(x), ...) # proportions
print(a)
print(b)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
t2 <- function(x, ...) {
b <- prop.table(t(x), ...) # proportions
}
print(a)
print(b)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
t <- function(x, ...) {
a <- table(x, ...) # counts
t2 <- function(x, ...) {
b <- prop.table(t(x), ...) # proportions
print(b)
}
print(b)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
t2 <- function(x, ...) {
b <- prop.table(a, ...) # proportions
print(b)
}
print(a)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
t2 <- function(x, ...) {
b <- prop.table(a, ...) # proportions
}
print(a)
print(b)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
t2 <- function(x, ...) {
b <- prop.table(a, ...) # proportions
print(b)
}
print(a)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
print(a)
t2 <- function(x, ...) {
b <- prop.table(a, ...) # proportions
print(b)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
t2 <- function(x, ...) {
b <- prop.table(a, ...) # proportions
}
print(a)
print(b)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
a <- table(x, ...) # counts
t2 <- function(x, ...) {
b <- prop.table(table(x, ...)) # proportions
}
print(a)
print(b)
}
with(data2, t(Q1, Q2))
t <- function(x, ...) {
table(x, ...) # counts
t <- function(x, ...) {
table(x, ...) # counts
}
with(data2, t(Q1, Q2))
t2 <- function(x, ...) {
t(x, ...)
prop.table(t, ...) # proportions
}
with(data2, t(Q1, Q2))
with(data2, t2(Q1, Q2))
t <- function(x, ...) {
table(x, ...) # counts
}
t2 <- function(x, ...) {
t(x)
prop.table(t(x), ...) # proportions
}
with(data2, t2(Q1, Q2))
t2 <- function(x, ...) {
prop.table(table(x, ...)) # proportions
}
with(data2, t2(Q1, Q2))
with(data2, t2(Q1, Q2), margin = 1)
with(data2, t2(Q1, Q2), margin = 2)
with(data2, t2(PPGENDER, Q3), margin = 2)
with(data2, t2(PPGENDER, Q3), margin = 1)
??prop.table
t2 <- function(x, ...) {
a <- table(x, ...)
prop.table(a, ...) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 1)
with(data2, t2(PPGENDER, Q3))
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)
rm(list = ls())
setwd("~/git/banana-phone/work")
library(dplyr)
library(tidyr)
library(ggplot2)
load("clean/cleaning_all.Rdata")
# set new names
names(data2) <- new_name
t2 <- function(x, ...) {
a <- table(x, ...)
prop.table(a, ...) # proportions
}
with(data2, t2(PPGENDER, Q3))
t <- function(x, ...) {
table(x, ...) # counts
}
with(data2, t(PPGENDER, Q3))
t2 <- function(x, ...) {
a <- table(x, ...)
prop.table(a) # proportions
}
with(data2, t2(PPGENDER, Q3))
with(data2, t2(PPGENDER, Q3), margin = 1)
with(data2, t2(PPGENDER, Q3), margin = 2)
t2 <- function(x, ...) {
a <- table(x)
prop.table(a, ...) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 2)
t2 <- function(x, ...) {
a <- table(x)
prop.table(a) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 2)
with(data2, t2(PPGENDER, Q3), margin = 1)
with(data2, (t2(PPGENDER, Q3), margin = 1))
t2 <- function(x, ...) {
prop.table(table(a)) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 1)
t2 <- function(x, ...) {
prop.table(table(x)) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 1)
t2 <- function(x, ...) {
prop.table(table(x, ...)) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 1)
with(data2, t2(PPGENDER, Q3), margin = 2)
t2 <- function(x, ...) {
margin.table(table(x, ...), ...) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 2)
with(data2, t2(PPGENDER, Q3))
with(data2, t2(Q3))
with(data2, t2(Q3, margin = 1))
with(data2, t2(PPGENDER, Q3, margin = 1))
t2 <- function(x, ...) {
margin.table(table(x, ...)) # proportions
}
with(data2, t2(PPGENDER, Q3, margin = 1))
with(data2, t2(PPGENDER, Q3), margin = 1))
with(data2, t2(PPGENDER, Q3), margin = 1)
t2 <- function(x, ...) {
margin.table(table(x), ...) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 1)
t2 <- function(x, ...) {
prop.table(table(x)) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 1)
t2 <- function(x, ...) {
prop.table(table(x), ...) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 1)
t2 <- function(x, ...) {
prop.table(table(x, ...)) # proportions
}
with(data2, t2(PPGENDER, Q3), margin = 1)
with(data2, t2(PPGENDER, Q3), margin = 2)
t2 <- function(x, ...) {
prop.table(table(x, ...)) # proportions
}
with(data2, t2(PPGENDER, Q3))
t <- function(x, ...) {
table(x, ...) # counts
}
t2 <- function(x, ...) {
prop.table(table(x, ...)) # proportions
}
t <- function(x) {
c(t(x), t2(x))
}
with(data2, t(PPGENDER, Q3))
t <- function(x, ...) {
c(t(x), t2(x))
}
with(data2, t(PPGENDER, Q3))
t <- function(x, ...) {
table(x, ...) # counts
prop.table(table(x, ...))
}
with(data2, t(PPGENDER, Q3))
t <- function(x, ...) {
print(table(x, ...)) # counts
print(prop.table(table(x, ...)))
}
with(data2, t(PPGENDER, Q3))
with(data2, t(Q1, PPGENDER))
data2 %>%
t(Q1, PPGENDER)
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)
rm(list = ls())
setwd("~/git/banana-phone/work")
library(dplyr)
library(tidyr)
library(ggplot2)
load("clean/cleaning_all.Rdata")
# set new names
names(data2) <- new_name
t <- function(x, ...) {
print(table(x, ...)) # counts
print(prop.table(table(x, ...)))
}
data2 %>%
t(Q1, PPGENDER)
with(data2, t(Q1, PPGENDER))
t <- function(x, ...) {
print(table(x, ...)) # counts
print(addmargins(prop.table(table(x, ...))))
}
with(data2, t(Q1, PPGENDER))
t <- function(x, ...) {
print(addmargins(table(x, ...))) # counts
print(addmargins(prop.table(table(x, ...))))
}
with(data2, t(Q1, PPGENDER))
with(data2, t(Q1, Q2))
t2 <- function(x, ...) {
addmargins(table(x, PPGENDER, ...))
}
with(data2, t2(Q1))
t2 <- function(x, ...) {
addmargins(table(x, PPGENDER))
}
with(data2, t2(Q1))
t2 <- function(x, y = PPGENDER, ...) {
addmargins(table(x, y))
}
with(data2, t2(Q1))
t2 <- function(x, y = PPGENDER) {
addmargins(table(x, y))
}
with(data2, t2(Q1))
with(data2, t2(x = Q1))
with(data2, t2(x = Q1, y))
with(data2, t2(x = Q1))
t2 <- function(x, y = PPGENDER) {
addmargins(table(x, y))
}
with(data2, t2(x = Q1))
View(data2)
t2 <- function(x, y = 'PPGENDER', ...) {
t = addmargins(table(x, y))
t
}
with(data2, t2(x = Q1))
with(data2, t2(x = 'Q1'))
t2 <- function(x, y = 'PPGENDER', ...) {
addmargins(table(x, y))
}
with(data2, t2(x = 'Q1'))
with(data2, t2(Q1))
with(data2, t2(Q1, y))
with(data2, t2(Q1, PPGENDER))
t2 <- function(x, y, ...) {
addmargins(table(x, y))
}
with(data2, t2(Q1, PPGENDER))
t2 <- function(x, y = PPGENDER, ...) {
addmargins(table(x, y))
}
with(data2, t2(Q1, y))
t2 <- function(x, y = data2$PPGENDER, ...) {
addmargins(table(x, y))
}
with(data2, t2(Q1, y))
t2 <- function(x, y, ...) {
addmargins(table(x, y))
}
x = Q1
y = PPGENDER
with(data2, t2(x, y))
x = data2$Q1
y = data2$PPGENDER
x = data2$Q1
y = data2$PPGENDER
with(data2, t2(x, y))
t2(x, y)
t <- function(x, ...) {
print(addmargins(table(x, ...))) # counts
print(addmargins(prop.table(table(x, ...))))
}
t2 <- function(x, y, ...) {
print(addmargins(table(x, y)))
print(addmargins(prop.table(table(x, y, ...))))
}
x = data2$Q1
y = data2$PPGENDER
t(x)
q = data2$Q1
t(q)
v = data2$PPGENDER
v1 = data2$Q1
v2 = data2$PPGENDER
t(v1)
t2(v1, v2)
t(x = 'a')
t(x, y)
t2(x, y)
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)
rm(list = ls())
setwd("~/git/banana-phone/work")
library(dplyr)
library(tidyr)
library(ggplot2)
load("clean/cleaning_all.Rdata")
# set new names
names(data2) <- new_name
t <- function(x, ...) {
print(addmargins(table(x, ...))) # counts
print(addmargins(prop.table(table(x, ...))))
}
t2 <- function(x, y, ...) {
print(addmargins(table(x, y)))
print(addmargins(prop.table(table(x, y, ...))))
}
x = data2$Q1
y = data2$PPGENDER
t(x, y)
t2(x, y)
y = data2$PPETHM
t(x, y)
t(y,x)
t(y)
t(x)
t(data2$Q1)
